apiVersion: v1
kind: Service
metadata:
  name: scaletestapp-service
  namespace: sprint-6
  labels:
    app: scaletestapp
    prometheus-monitored: "true"
spec:
  selector:
    app: scaletestapp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort